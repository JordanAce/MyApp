import { exportAllDeclaration } from '@babel/types'
import React from 'react'
import renderer from 'react-test-renderer'
// import App from '../src/app.js'

describe('It should test my app', () => {
  test('One Equals One', () => {
    expect(1).toEqual(1);
  })
})